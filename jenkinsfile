pipeline {
 agent {
         label {
		        label 'built-in'
				customWorkspace '/mnt/project'
		 
		 }
 
 
 }
  
      stages {
	  
	            stage ('git-colne') {
				
				     steps {
					           sh "rm -rf *"
				              sh "git clone https://github.com/vishwajitdhanawade/game-of-life.git"
							  
				               }
							}
				                         
                                              }
     }
